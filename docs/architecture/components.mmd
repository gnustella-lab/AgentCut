flowchart LR
    USER[User\nedit brief] --> UI[Agent Director\nstatic browser UI]

    subgraph Browser[Local AgentCut runtime]
        PROJECT[Project state\nmedia + sequence + persistence]
        CONTEXT[Context builder\nagentcut-context-v2]
        DIRECTOR[Local Director\nintent + blueprint + mission]
        POLICY[Approval policy\none approval for full mission]
        TIMELINE[Timeline engine\nlocked tracks + safe operations]
        HISTORY[History\nundo / redo / manifest]
        PREVIEW[Local preview\nobject URLs]
    end

    UI --> CONTEXT
    CONTEXT --> DIRECTOR
    PROJECT --> CONTEXT
    DIRECTOR --> POLICY
    POLICY --> TIMELINE
    TIMELINE --> PROJECT
    TIMELINE --> HISTORY
    PROJECT --> PREVIEW
    HISTORY --> UI

    DIRECTOR -. held until evidence .-> ANALYSIS[Future transcript / audio / vision / render workers]
